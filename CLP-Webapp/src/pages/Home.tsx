/**
 * Home page — the CLP landing page.
 *
 * This page should stay a thin composition of section components
 * from `src/components/landing/*`. Do not implement section markup
 * directly in this file — build each section as its own component
 * and import it here.
 *
 * Sections are intentionally not implemented yet (initial project
 * setup phase). They will be added one at a time in future prompts.
 */
function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-surface">
      <p className="text-sm text-text-secondary">CLP project foundation ready.</p>
    </main>
  )
}

export default Home
