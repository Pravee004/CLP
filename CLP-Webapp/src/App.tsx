import Home from '@/pages/Home'

/**
 * Root application component.
 *
 * Keep this file minimal — it wires up top-level composition only
 * (routing, providers, layout shell) once those exist. Page content
 * belongs in `src/pages/*`, not here.
 */
function App() {
  return <Home />
}

export default App
